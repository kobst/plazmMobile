# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'plazmMobile (iOS)' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for plazmMobile (iOS)

 #pods for authentication
 pod 'Amplify'
 pod 'AmplifyPlugins/AWSCognitoAuthPlugin'
 

  pod 'PureLayout', :modular_headers => true
  pod 'SideMenu', '6.4.8', :modular_headers => true
  pod 'iCarousel', :modular_headers => true
end
